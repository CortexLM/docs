---
title: "Ask, Plan, Agent"
description: "A turn carries the choice; the session row is the default."
---

Ask, Plan, and Agent choose how a Code turn behaves. Ask and Plan do not
edit files or run shell. Plan can write a mermaid plan. Agent can change
the repository.

A follow-up can pick a different mode. Web Code has no model picker — web
is Cloud only.
