---
title: "Public API"
description: "What this site documents about the Cortex API: the RFC 9457 problem-document format and the public error-code catalog. Sign in via the app."
---

This site is **end-user product documentation** for Cortex Chat, Cortex Code, Cortex Bot, and the Cortex CLI.

**Sign in at [cortex.foundation](https://cortex.foundation).** The CLI and desktop app use the same account. This site does not document login, sessions, or identity-provider wire protocol.

## What is public here

- [Problem catalog](/problems) — RFC 9457 `type` URIs (`https://docs.cortex.foundation/problems/{code}`)
- [Errors](/api/errors) — how a problem document is shaped
- Product guides: [Chat](/chat), [Code](/code), [Bot](/bot), [CLI](/cli)

Connect apps and GitHub from **Settings** and **Customize** in the product. There is no public plugin webhook catalog on this site.

## What is not here

- Authentication, session cookies, device codes, or identity-provider internals
- The app's private session API
- An inference Platform API — it has no public endpoints, and none are documented here

## Using the catalog

Branch on `code`, not on `title` or `detail`. The `code` member is the contract; it mirrors the `ErrorCode` enum in the API types package, and every value has a page under `/problems/{code}`. The `type` member dereferences to that page, so a client can show an explanation without shipping its own copy of the list.
